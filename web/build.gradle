plugins {
    id 'org.springframework.boot' version '2.5.5'
}

group 'com.karnyshov'
version '1.0'

dependencies {
    implementation project(':service')
}

bootJar {
    archiveName 'certificates.jar'
}